/**
 * @format
 */

import {AppRegistry} from 'react-native';
import ScreenManager from './components/ScreenManager';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ScreenManager);
